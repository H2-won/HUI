VanillaTilt.init(document.querySelectorAll(".productBox"), {
    max: 20,
    speed: 300
});

const productBoxs = document.querySelectorAll(".productBox");

mouseHovering(productBoxs[0], '', '#F68981', '3em');
mouseHovering(productBoxs[1], '', '#A68FD2', '3em');
mouseHovering(productBoxs[2], '', '#0061EB', '3em');